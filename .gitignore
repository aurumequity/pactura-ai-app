# Node
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Next.js
.next/
out/
.vercel/

# Environment variables (do not commit secrets)
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Logs
logs/
*.log
firebase-debug.log
firestore-debug.log

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# TypeScript
*.tsbuildinfo

# lockfiles we do not track
/frontend/package-lock.json

# Scripts for building project that we do not track
/scripts/

# API dist folder
/api/.env

# Firebase emulator data (optional, if you want to keep emulator state out of version control)
/emulator-data/

# Service account keys
service-account.json

# CORS configuration (if you have a custom CORS config file)
cors.json

# Ignore package-lock.json if we are using npm, but keep it if we are using pnpm
package-lock.json
